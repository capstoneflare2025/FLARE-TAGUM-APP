<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Logo -->
    <ImageView
        android:id="@+id/logo"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:src="@drawable/ic_logo"
        android:layout_gravity="center"
        android:layout_marginStart="115dp"
        android:layout_marginTop="30dp"
        android:contentDescription="Logo" />

    <!-- Floating Text: Click to back home -->
    <TextView
        android:id="@+id/tv_back_home"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Click logo to Login\Register!"
        android:textSize="14sp"
        android:textColor="?android:textColorPrimary"
        android:textStyle="bold"
        android:layout_below="@id/logo"
        android:layout_centerHorizontal="true"
        android:clickable="true"
        android:focusable="true" />

    <!-- Curved White Background (Smaller) -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/tv_back_home"
        android:layout_marginTop="20dp"
        android:layout_alignParentBottom="true">

        <LinearLayout
            android:id="@+id/curved_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/curved_bg_login"
            android:padding="32dp"
            android:orientation="vertical"
            android:elevation="10dp">

            <!-- Name Field -->
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Name:"
                android:textColor="@color/white"
                android:textSize="16sp"
                android:layout_marginBottom="8dp" />

            <EditText
                android:id="@+id/name"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="Enter your name"
                android:background="@color/gray"
                android:textColor="@android:color/black"
                android:textColorHint="@color/black"
                android:padding="14dp"
                android:layout_marginBottom="20dp"
                android:inputType="textPersonName" />

            <!-- Location Field -->
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Location:"
                android:textColor="@color/white"
                android:textSize="16sp"
                android:layout_marginBottom="8dp" />

            <EditText
                android:id="@+id/location"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="Enter your location"
                android:background="@color/gray"
                android:textColor="@android:color/black"
                android:textColorHint="@color/black"
                android:padding="14dp"
                android:layout_marginBottom="20dp"
                android:inputType="textPostalAddress" />

            <!-- Fire Report Field -->
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Fire Report:"
                android:textColor="@color/white"
                android:textSize="16sp"
                android:layout_marginBottom="8dp" />

            <EditText
                android:id="@+id/fireReport"
                android:layout_width="match_parent"
                android:layout_height="150dp"
                android:hint="Please complete information\n1.What time fire start\n2.How many houses affected"
                android:background="@color/gray"
                android:textColor="@android:color/black"
                android:textColorHint="@color/black"
                android:padding="14dp"
                android:gravity="top|start"
                android:inputType="textMultiLine"
                android:minLines="5"
                android:layout_marginBottom="30dp" />

            <!-- Send Report Button -->
            <Button
                android:id="@+id/sendReport"
                android:layout_width="250dp"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:text="SEND REPORT!"
                android:textAllCaps="false"
                android:textColor="@android:color/white"
                android:backgroundTint="#E00024"
                android:layout_marginBottom="16dp" />

        </LinearLayout>
    </ScrollView>

</RelativeLayout>
